export function part1 (input: string) {
  const crabSubmarines = input.split(',').map(Number);
  
  crabSubmarines.forEach(number => {
    let sum = 0;
    
  })
}

export function part2 (input: string) {

}
